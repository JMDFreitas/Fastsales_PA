import { LightningElement, api } from "lwc";

export default class QuickActionsDemo extends LightningElement {
    @api invoke() {
        // Do whatever we want in here.
        // Call out to apex or other components etc
    }
}